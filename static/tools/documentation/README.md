# Provider Documentation Metadata

This directory contains one JSON document for each configured provider. Each
records official documentation, API and archive references, data dictionaries,
licensing and attribution, rate limits, update notes, support links, and
verification dates.

Provider identifiers must match files in providers, schemas, policies,
examples, and tests. Prefer authoritative sources and distinguish confirmed
provider behavior from implementation assumptions. Never include credentials.
