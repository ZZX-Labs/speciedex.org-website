"""Extension point for the providers endpoint."""
from __future__ import annotations
from typing import Any


def register(router: Any, services: Any) -> None:
    """Register project-specific providers routes when needed."""
    return None
